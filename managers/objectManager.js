'use strict';

module.exports = class ObjectManager{    
    constructor(sequelize) {
        this.sequelize = sequelize;

        /* manage :
         - object
         - repository
         - source
         */
    }
};